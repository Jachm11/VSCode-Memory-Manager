\begin{quote}
Start a promise chain \end{quote}


\href{http://cryto.net/~joepie91/blog/2016/05/11/what-is-promise-try-and-why-does-it-matter/}{\texttt{ How is it useful?}}

\doxysection*{Install}


\begin{DoxyCode}{0}
\DoxyCodeLine{\$ npm install p-\/try}
\end{DoxyCode}


\doxysection*{Usage}


\begin{DoxyCode}{0}
\DoxyCodeLine{const pTry = require('p-\/try');}
\DoxyCodeLine{}
\DoxyCodeLine{(async () => \{}
\DoxyCodeLine{    try \{}
\DoxyCodeLine{        const value = await pTry(() => \{}
\DoxyCodeLine{            return synchronousFunctionThatMightThrow();}
\DoxyCodeLine{        \});}
\DoxyCodeLine{        console.log(value);}
\DoxyCodeLine{    \} catch (error) \{}
\DoxyCodeLine{        console.error(error);}
\DoxyCodeLine{    \}}
\DoxyCodeLine{\})();}
\end{DoxyCode}


\doxysection*{A\+PI}

\doxysubsection*{p\+Try(fn, ...\+arguments)}

Returns a {\ttfamily Promise} resolved with the value of calling {\ttfamily fn(...\+arguments)}. If the function throws an error, the returned {\ttfamily Promise} will be rejected with that error.

Support for passing arguments on to the {\ttfamily fn} is provided in order to be able to avoid creating unnecessary closures. You probably don\textquotesingle{}t need this optimization unless you\textquotesingle{}re pushing a {\itshape lot} of functions.

\doxysubsubsection*{fn}

The function to run to start the promise chain.

\doxysubsubsection*{arguments}

Arguments to pass to {\ttfamily fn}.

\doxysection*{Related}


\begin{DoxyItemize}
\item \href{https://github.com/sindresorhus/p-finally}{\texttt{ p-\/finally}} -\/ {\ttfamily Promise\#finally()} ponyfill -\/ Invoked when the promise is settled regardless of outcome
\item \href{https://github.com/sindresorhus/promise-fun}{\texttt{ More…}}
\end{DoxyItemize}

\doxysection*{License}

M\+IT © \href{https://sindresorhus.com}{\texttt{ Sindre Sorhus}} 